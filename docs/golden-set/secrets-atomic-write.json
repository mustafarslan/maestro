{
  "name": "secrets-atomic-write",
  "description": "The secret store rewrites the whole JSON file in place, so an interrupted write loses every key rather than one; and a corrupt file throws out of whatever was resolving a key.",
  "target": "~/.maestro/fixture-repos/secrets-atomic-write",
  "split": "train",
  "expected": [
    {
      "match": "/(?=[\\s\\S]*(secret|api key|credential|keychain|keys?\\.json|key store))(?=[\\s\\S]*(atomic|in place|partial(ly)?|interrupt|crash|truncat|temp(orary)? file|rename|lose (all|every)|every key|corrupt))/i",
      "file": "packages/llm/src/keys.ts",
      "description": "writing the whole file in place can lose every key, not one - both halves must be said"
    }
  ],
  "forbidden": []
}
