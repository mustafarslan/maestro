{
  "name": "checkout-cache-merge",
  "description": "refreshCheckout copies the new checkout into the cached image with `docker cp`, which merges and never deletes, so the analysed tree is previous-union-current.",
  "target": "~/.maestro/fixture-repos/checkout-cache-merge",
  "split": "val",
  "expected": [
    {
      "match": "/(?=[\\s\\S]*(checkout|docker cp|copied|copy|previous (pull request|PR|review|checkout)))(?=[\\s\\S]*(merge|never (deletes|removes)|not (deleted|removed|cleared)|left ?over|stale|union|still (there|present)|deleted file))/i",
      "file": "packages/sandbox/src/docker.ts",
      "description": "the previous checkout is not removed before the new one is copied in - both halves must be said"
    }
  ],
  "forbidden": []
}
